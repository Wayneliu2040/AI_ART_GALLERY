# Azure Blob Storage Notes

This folder contains helper assets for Azure Blob Storage.

## Current Asset

- `create-container.ps1`

Use the script after logging in with Azure CLI:

```powershell
az login
.\create-container.ps1 -StorageAccountName <your-storage-account> -ResourceGroupName <your-resource-group>
```
